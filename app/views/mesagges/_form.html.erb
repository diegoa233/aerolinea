<%= form_for(@mesagge) do |f| %>
  <% if @mesagge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mesagge.errors.count, "error") %> prohibited this mesagge from being saved:</h2>

      <ul>
      <% @mesagge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_field :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
